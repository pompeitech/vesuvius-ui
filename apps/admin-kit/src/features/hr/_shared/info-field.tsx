// Moved to `apps/admin-kit/src/components/info-field.tsx` once Project Management
// needed the same label+value pattern too — re-exported here so every
// existing `../_shared/info-field` / `../../_shared/info-field` import
// across the HR pages keeps working unchanged.
export { InfoField } from '../../../components/info-field'
