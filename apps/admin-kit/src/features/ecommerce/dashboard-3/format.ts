/** Radial-chart segment colors for the Order Status card — unique to this dashboard's chart type. */
export const ORDER_STATUS_COLOR: Record<string, string> = {
  processing: 'var(--chart-2)',
  shipped: 'var(--chart-4)',
  delivered: 'var(--success)'
}
