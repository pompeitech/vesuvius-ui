---
title: Icon Button
---

# Icon Button

An accessible compact button for icon-only actions.

<ComponentPreview code={`<IconButton aria-label="Settings"><SettingsIcon /></IconButton>`}>
  <IconButton aria-label="Settings"><SettingsIcon /></IconButton>
</ComponentPreview>

Always provide an `aria-label` when the button has no visible text.
