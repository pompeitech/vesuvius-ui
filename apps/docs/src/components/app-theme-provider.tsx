'use client'

// Same reasoning as mdx-components.tsx — and literally the exact pattern
// documented in content/en/docs/installation.mdx's own Next.js App Router
// tab, dogfooded here for the same reason.
export { ThemeProvider, Toaster } from '@pompeitech/vesuvius-ui'
